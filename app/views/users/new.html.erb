<h1>Upload Excel</h1>
<div>
  <%= form_for(User.new,remote: true, html: {class: "upload_form"}) do |f|%>
  <%= f.file_field :list, accept: ".xlsx, .xls, .csv",class: "upload_file"  %>
  <%= f.submit :upload  %>
  <% end %>
</div>
<div class="main_listing">

</div>
